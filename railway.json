{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "python manage.py migrate && python manage.py collectstatic --noinput && python manage.py seed_platforms && python manage.py create_workspace 'Default Workspace' --admin-email demo@tuxonomy.com --create-admin --skip-existing",
    "startCommand": "gunicorn --timeout 600 --worker-connections 1000 --max-requests 1000 --max-requests-jitter 100 --bind 0.0.0.0:$PORT main.wsgi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
